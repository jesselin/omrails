<% if user_signed_in? %>

	<div class="hero-unit">
		<h1>You are logged in</h1>
	</div>

<% else %>

	<div class="hero-unit">
		<h1>Welcome to OMRails!</h1>
		<h2>Pin your stuff today!<h2>
		<p>
			<%= button_to "Sign Up Now!", new_user_registration_path, method: :get,class: "btn btn-success btn-large" %>
		</p>
	</div>

<% end %>